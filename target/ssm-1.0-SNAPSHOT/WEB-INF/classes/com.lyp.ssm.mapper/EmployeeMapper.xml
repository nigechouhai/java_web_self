<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lyp.ssm.mapper.EmployeeMapper">

    <!--List<Employee> getAllEmployee();-->
    <select id="getAllEmployee" resultType="Employee">
        select * from temple.t_emp
    </select>

    <!--void setEmployee();-->
    <update id="updateEmployee" parameterType="Employee">
        update from temple.t_emp where empid = ${empId} set password = ${password}
    </update>

    <!--void deleteEmployee();-->
    <delete id="deleteEmployee" parameterType="Employee">
        delete * from temple.t_emp where empid = ${empId}
    </delete>
</mapper>